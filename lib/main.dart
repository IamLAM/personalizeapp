import 'package:flutter/material.dart';

void main() {
  runApp(
    const MyApp(
      MaterialApp(
        home: Scaffold(
          title: Text(""),
          appBar: AppBar(backgroundColor: Colors.amber),
        ),
      ),
    ),
  );
}
